import { ReviewedPipe } from './reviewed.pipe';

// TODO: write tests

describe('ReviewedPipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewedPipe();
    expect(pipe).toBeTruthy();
  });
});
